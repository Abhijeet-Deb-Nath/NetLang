# Fixtures

This directory contains internal inputs that are not part of the public example set.

## Layout

- `parser/` holds small parser and semantic fixtures
- `legacy/` holds historical network definitions kept only for internal regression or comparison

Public, user-facing sample networks belong in `examples/`, not here.
