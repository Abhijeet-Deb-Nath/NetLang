# Binaries

This directory stores built executables and dynamic libraries produced from generated NetLang output.

## Intended Use

- smoke tests
- local development runs
- temporary build artifacts

## Current Caveat

Do not treat checked-in or locally produced binaries as benchmark evidence by themselves.

Evaluation must use the protocol in `docs/BENCHMARKING.md`.
