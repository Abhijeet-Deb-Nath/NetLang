# Legacy Tools

The actively supported workflow lives in:

- `run_eval.py`
- `run_eval_matrix.py`
- `benchmark.c`
- `test_network.c`

Older ad hoc profiling and benchmarking scripts were removed during repo cleanup because they duplicated the current evaluation pipeline and no longer matched the supported repo layout.
