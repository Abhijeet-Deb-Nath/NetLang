# NetLang Compiler Architecture

The publication-oriented architecture figure now lives in
[flowchart.dot](/c:/Users/Ankon/Desktop/Projects/Compiler_Project/flex_work/docs/flowchart.dot).

Recommended rendering options:

- Graphviz Online: paste the contents of `docs/flowchart.dot` into `https://dreampuf.github.io/GraphvizOnline/`
- Local Graphviz:

```powershell
dot -Tsvg docs/flowchart.dot -o docs/flowchart.svg
dot -Tpdf docs/flowchart.dot -o docs/flowchart.pdf
```

Use `SVG` or `PDF` for paper figures. The earlier Mermaid draft was removed because it was not a good fit for publication-quality layout.
